<div class="modal-overlay">
  <div class="flashcard-container animate-pop-in">
    
    <div class="flashcard-header">
      <span class="badge-category">Mana Challenge</span>
      <span class="timer-icon">⏱️</span>
    </div>

    <div class="flashcard-body">
      <h2 class="question-text"><%= question_text %></h2>
    </div>

    <div class="flashcard-form-container">
      <%= form_with url: answer_game_path(id: game_id), method: :post do |f| %>
        
        <div class="input-group">
          <%= text_field_tag :answer, nil, 
                placeholder: "Type your answer...", 
                class: "flashcard-input", 
                autofocus: true, 
                autocomplete: "off" 
          %>
          
          <%= f.submit "Submit Answer", 
                class: "btn-submit",
                data: { turbo_submits_with: "Checking..." } 
          %>
        </div>

      <% end %>
    </div>

    <div class="flashcard-footer">
      <p>Answer correctly to gain Mana!</p>
    </div>
  </div>
</div>