<!DOCTYPE html>
<html data-bs-theme="dark">
  <head>
    <title><%= content_for(:title) || "Flashcard Game" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Flashcard Game">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true%>
  </head>

  <body class="bg-body">
  <nav class="navbar navbar-expand bg-secondary">
    <div class="container">
      <a class="navbar-brand text-wrap" href="/">Hack & Flash</a>

      <div class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <span class="navbar-text me-3">
            <%= current_user.username %>
          </span>
          <%= button_to "Log out",
          destroy_user_session_path,
          method: :delete,
          form: { data: { turbo: true} },
          class: "btn btn-primary text-nowrap" %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "btn btn-outline-light btn-sm me-2" %>
          <%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary btn-sm" %>
        <% end %>
      </div>
    </div>
  </nav>

  <main class="container mt-5 mb-5 bg-dark">
    <%= yield %>
  </main>
</body>

</html>
